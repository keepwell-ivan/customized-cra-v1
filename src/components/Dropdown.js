import { useState } from 'react'

export default function Dropdown() {
	const [enabled, setEnabled] = useState(false)

	return <div tabIndex={0} role="button" onKeyDown={() => {}} onClick={() => {}}></div>
}
